{
  "id": "starship",
  "version": "1.0.1",
  "name": "starship",
  "description": "Installs the starship cross-shell prompt and configures the bash hook for all users, on both Debian/Ubuntu (/etc/bash.bashrc) and RPM distros (/etc/profile.d)",
  "options": {
    "version": {
      "type": "string",
      "default": "1.26.0",
      "description": "Version of starship to install"
    },
    "installShellHook": {
      "type": "boolean",
      "default": true,
      "description": "Add 'eval \"$(starship init bash)\"' to the system shell startup file. Turn this off if a dotfiles repo initialises starship itself."
    }
  }
}
