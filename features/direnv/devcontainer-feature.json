{
  "id": "direnv",
  "version": "1.1.0",
  "name": "direnv",
  "description": "Installs direnv and configures the shell hook for all users, on both Debian/Ubuntu (/etc/bash.bashrc) and RPM distros (/etc/profile.d)",
  "options": {
    "version": {
      "type": "string",
      "default": "2.37.1",
      "description": "Version of direnv to install"
    }
  }
}
