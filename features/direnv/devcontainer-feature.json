{
  "id": "direnv",
  "version": "1.2.1",
  "name": "direnv",
  "description": "Installs direnv and configures the shell hook for all users, on both Debian/Ubuntu (/etc/bash.bashrc) and RPM distros (/etc/profile.d)",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version of direnv to install. \"latest\" resolves the newest stable tag at build time; give an exact version to pin.",
      "proposals": [
        "latest",
        "2.37",
        "2.37.1"
      ]
    }
  }
}
