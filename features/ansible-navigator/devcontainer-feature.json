{
  "id": "ansible-navigator",
  "version": "1.6.0",
  "name": "ansible-navigator",
  "description": "Installs ansible-navigator as a standalone pipx package. Only ansible-navigator reaches PATH: ansible-core and ansible-lint come along as dependencies but stay unexposed, so nothing competes with the Execution Environment, which is where the collections live. Needs a container runtime (Docker/Podman).",
  "installsAfter": [
    "ghcr.io/devcontainers/features/python"
  ]
}
