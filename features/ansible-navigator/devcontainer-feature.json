{
  "id": "ansible-navigator",
  "version": "1.4.0",
  "name": "ansible-navigator",
  "description": "Installs ansible-navigator as a standalone pipx package with --include-deps, which also exposes ansible, ansible-playbook, ansible-galaxy and ansible-lint from the same venv. Needs a container runtime (Docker/Podman) for Execution Environments; no separate Ansible install is required.",
  "installsAfter": [
    "ghcr.io/devcontainers/features/python"
  ]
}
