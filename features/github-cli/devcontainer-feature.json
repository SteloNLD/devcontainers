{
  "id": "github-cli",
  "version": "1.1.1",
  "name": "GitHub CLI",
  "description": "Installs the GitHub CLI (gh) with dnf where available, falling back to the release tarball. The upstream devcontainers/features/github-cli is apt-only, so it cannot run on an RPM base.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version to install. \"latest\" resolves the newest stable tag at build time; give an exact version to pin.",
      "proposals": [
        "latest",
        "2.97",
        "2.97.0"
      ]
    },
    "useRelease": {
      "type": "boolean",
      "default": false,
      "description": "Force the pinned release tarball even when dnf is available. Use when you need an exact version rather than the distro's."
    },
    "installManPages": {
      "type": "boolean",
      "default": false,
      "description": "Install gh's man pages (229 of them, ~950 KB). Release-tarball path only; dnf decides for itself. 'gh --help' works either way."
    }
  }
}
